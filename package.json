{
  "name": "my-screen-resolution",
  "version": "1.0.0",
  "description": "A website to see your Screen and Browser resolution.",
  "main": "index.js",
  "repository": "git@github.com:BrOrlandi/my-screen-resolution.git",
  "author": "Bruno Orlandi",
  "license": "MIT",
  "scripts": {
    "build": "rm -rf ./dist && parcel build ./src/index.html --public-url /my-screen-resolution --no-cache",
    "build-dev": "yarn build --public-url / --no-cache",
    "start": "parcel ./src/index.html -p 8080"
  },
  "devDependencies": {
    "parcel-bundler": "^1.9.7"
  }
}
